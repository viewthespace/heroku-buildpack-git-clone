#!/usr/bin/env bash

GIT_PARENT_DIR=$BUILD_DIR/tmp/deploy
GIT_URL=`cat $ENV_DIR/$GIT_URL`

#start from scratch
rm -r -f $GIT_PARENT_DIR

mkdir -p $GIT_PARENT_DIR

echo "-----> cloning git repo"
cd $GIT_PARENT_DIR
git clone $GIT_URL
echo "-----> done cloning git repo"




